use std::fs;

fn main() {
    let file = fs::read_to_string("/home/operaho/Advent/D3/adv.txt").unwrap();
    let v = Vec::new();

    for i in file.lines() {
        v.push(i);
    }

    for j in v {
        
    }

}
